PORT=3000
DB_PATH=./data/dev.sqlite
WEBHOOK_WORKER_INTERVAL_MS=1000
WEBHOOK_DELIVERY_TIMEOUT_MS=5000
WEBHOOK_RECEIVER_PORT=4000
# Optional: pin the seed script's demo merchant to a fixed, shareable API key
# instead of a random one (useful when hosting a public demo link).
# SEED_API_KEY=demo_sk_fractalpay_shared_key
